<div class="container m-t-20 m-b-20">
  <div class="row">
    <div class="col-lg-12">
      <p id="notice"><%= notice %></p>

      <h1>All Projects</h1>

      <% @posts.each do |post| %>
        <h2><%= link_to post.title, post_path(post.slug) %></h2>
        <div class="row">
          <div class="col-lg-4">
            <!-- post.main_image -->
            <%= image_tag post.article_image, class: "img-responsive" %>
          </div>
          <div class="col-lg-8">
            <%= post.content.html_safe %>
          </div>
      </div>
      <% end %>


<!--       <table class="table">
        <thead>
          <tr>
            <th>Category</th>
            <th>Title</th>
            <th>Content</th>
            <th>Teaser</th>
            <th>Slug</th>
            <th>Published on</th>
            <th>Published</th>
            <th>Admin user</th>
            <th>Article image</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @posts.each do |post| %>
            <tr>
              <td><%= post.category.slug %></td>
              <td><%= post.title %></td>
              <td><%= truncate(post.content, length: 100) %></td>
              <td><%= truncate(post.teaser, length: 100) %></td>
              <td><%= post.slug %></td>
              <td><%= post.published_on %></td>
              <td><%= post.published %></td>
              <td><%= post.admin_user.email %></td>
              <td><%= image_tag post.article_image, class: "img-responsive" %></td>
              <td><%= link_to 'Show', post_path(post) %></td>
              <td><%= link_to 'Edit', edit_post_path(post) %></td>
              <td><%= link_to 'Destroy', post_path(post), method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
 -->
      <br>

      <%= link_to 'New Post', new_post_path, class: "btn btn-success" %>
    </div>
  </div>
</div>