<div class="container-fluid">
  <div class="row">
    <div class="col-xs-12">
      <%= image_tag @setting.home_image, class: "img-fluid" %>
    </div>
  </div>
</div>

<div class="container-fluid m-t-20 m-b-20">
  <div class="container">
    <div class="row text-center">
      <div class="col-xs-12">
        <h1 class="site-title">
          <%= @setting.title %></h1>
        <h2 class="site-subtitle"><%= @setting.subtitle %></h2>
        <hr>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-12">
        <%= @setting.description.html_safe %>
      </div>
    </div>
  </div>
</div>

<div class="container recent">
  <div class="container">
    <div class="row text-center">
      <div class="col-xs-12">
        <h2><%= @setting.projects_title %></h2>
        <hr>
      </div>
    </div>
    <div class="row mt-3">
      <% if !@posts.nil? %>
        <% @posts.each do |post| %>

            <div class="col-md-4">
              <%= image_tag post.article_image, class: "img-responsive img-thumbnail mb-3" %>
            </div>
            <div class="col-md-8">
              <h2 class="project-title"><%= link_to post.title, post_path(post), class: "project-title" %></h2>
              <div class="post-details">
                <span class="glyphicon glyphicon-time"></span>
                <% if post.published_on %>
                  <%= post.published_on.strftime("%B %d, %Y") %>
                   /
                <% end %>
                <%= link_to post.category.name, category_path(post.category) %>
              </div>
              <%= post.teaser.html_safe %>
              <br>
              <%= link_to 'Read More', post_path(post), class: "btn btn-info" %>
              <% if current_admin_user %>
                 |
                <%= link_to 'Edit', edit_post_path(post), class: "btn btn-warning" %> |
                <%= link_to 'Destroy', post_path(post), method: :destroy, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
              <% end %>
              <br>
            </div>
          <hr />
        <% end %>
      <% end %>
    </div>
  </div>
</div>

<div class="container recent">
  <div class="container">
    <div class="row text-center">
      <div class="col-xs-12">
        <h2>Services We Offer</h2>
        <hr>
      </div>
    </div>
    <div class="row mt-3">
      <% if !@services.nil? %>
        <% @services.each do |service| %>

            <div class="col-md-4">
              <%= image_tag service.article_image, class: "img-responsive img-thumbnail mb-3" %>
            </div>
            <div class="col-md-8">
              <h2 class="project-title"><%= link_to service.title, service_path(service), class: "project-title" %></h2>
              <div class="service-details">
                <span class="glyphicon glyphicon-time"></span>
                <% if service.published_on %>
                  <%= service.published_on.strftime("%B %d, %Y") %>
                   /
                <% end %>
                <%= link_to service.category.name, category_path(service.category) %>
              </div>
              <%= service.teaser.html_safe %>
              <br>
              <%= link_to 'Read More', service_path(service), class: "btn btn-info" %>
              <% if current_admin_user %>
                |
                <%= link_to 'Edit', edit_service_path(service), class: "btn btn-warning" %> |
                <%= link_to 'Destroy', service_path(service), method: :destroy, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
              <% end %>
              <br>
            </div>
          <hr />
        <% end %>
      <% end %>

    </div>
  </div>
</div>
